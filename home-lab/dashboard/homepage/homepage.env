TZ=America/Los_Angeles
# Homepage rejects any request whose Host header isn't in this list
# (anti DNS-rebinding check). Comma-separate every host:port you'll
# actually use to reach it, e.g. 192.168.1.50:10000,nas.local:10000
HOMEPAGE_ALLOWED_HOSTS=___REPLACE_ME_HOST_AND_PORT___
